// pode ser usado para configura valores que se repetem, além de value podemos usar também constant, a diferença é que constant pode ser inserida em provider.
manutencaoEventosApp.constant("configLength", {
    defaultLength: 10
});